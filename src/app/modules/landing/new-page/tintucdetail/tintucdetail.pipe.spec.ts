import { TintucdetailPipe } from './tintucdetail.pipe';

describe('TintucdetailPipe', () => {
  it('create an instance', () => {
    const pipe = new TintucdetailPipe();
    expect(pipe).toBeTruthy();
  });
});
